import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'cfa-express---convertisseur-ultra-rapide',
  webDir: 'dist'
};

export default config;
